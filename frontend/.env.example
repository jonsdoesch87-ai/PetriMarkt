# Frontend Environment Variables (Vercel)
# Copy this file to .env.local for local development

# Backend API URL (leave empty for local development, set for production)
# Example: https://your-backend.railway.app
VITE_BACKEND_URL=

# Note: Firebase configuration is hardcoded in frontend/src/firebase/config.ts
# If you need to use different Firebase projects for dev/prod, you should:
# 1. Move firebase config to environment variables
# 2. Add VITE_FIREBASE_API_KEY, VITE_FIREBASE_AUTH_DOMAIN, etc.
